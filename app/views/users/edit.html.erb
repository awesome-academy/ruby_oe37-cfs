<% provide :title, t("edit.title") %>
<% provide :button_text, t("edit.save_c") %>
<div class="row">
  <%= render template: "users/show" %>
</div>

<h1 class="center"><%= t("edit.edit_title") %></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
   <% if current_user?(@user) %>
      <%= render "form" %>
    <% else %>
      <%= render "user_delete" %>
    <% end %>
  </div>
</div>
